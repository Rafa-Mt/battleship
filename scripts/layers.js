const layers = Object.freeze({
    important: 0, // Not reccomended to use
    popups: 1,
    ui: 2,
    particles: 3,
    ships: 4,
    boardOverlay: 5,
    board: 6,
    background: 7,
});

export default layers;